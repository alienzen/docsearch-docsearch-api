# ── docsearch-api — dépendances Python ───────────────────────
# API REST de recherche avec filtrage ACL

# Elasticsearch 9.x
elasticsearch==9.4.1

# API REST
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0

# Cache
redis==5.2.0

# Upload fichiers (aperçu documents)
python-multipart==0.0.12

# Appels HTTP asynchrones (option RAG)
httpx==0.28.0

# Résolution des groupes Active Directory / LDAP
# ldap3 est une implémentation pure Python — aucune dépendance
# système (pas besoin de libldap-dev ni du paquet apt ldap-utils)
ldap3==2.9.1

# Panneau d'administration : lecture de l'état du groupe de consumers
# Kafka (workers actifs, lag = progression de l'indexation) et
# déclenchement d'un scan depuis l'API (publication sur Kafka)
kafka-python-ng==2.2.3

# Export XLSX de l'historique des recherches (/admin/search-logs/export)
# et des résultats de recherche (/search/export)
openpyxl==3.1.5

# Export DOCX des résultats de recherche (/search/export)
python-docx==1.1.2

# Chiffrement symétrique des DSN SQL enregistrés depuis le panneau
# d'administration (sql_dsn_registry.py) — jamais stockés en clair dans Redis.
# Porte aussi les clés RS256 qui signent les jetons de session (auth/tokens.py).
cryptography==49.0.0

# ── Authentification (voir app/auth/) ────────────────────────
# Jetons de session RS256 + JWKS. L'extra [crypto] tire l'implémentation
# asymétrique — sans lui, PyJWT ne sait signer qu'en HMAC.
pyjwt[crypto]==2.10.1

# Hachage des mots de passe des comptes de secours locaux. Argon2id
# uniquement, jamais bcrypt. Roue précompilée, aucun compilateur requis.
argon2-cffi==25.1.0

# Acceptation des tickets Kerberos (SPNEGO) pour la connexion automatique.
# ⚠️  Pas de roue précompilée : se compile contre libkrb5-dev, installé puis
# purgé dans la même couche du Dockerfile. C'est pourquoi cette ligne ne
# s'installe PAS dans un environnement virtuel d'hôte sans les en-têtes
# krb5 — auth/kerberos.py importe donc gssapi tardivement, pour rester
# importable (et testable) là où la bibliothèque manque.
gssapi==1.9.0
